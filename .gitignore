node_modules/
data/.capture.log
# Raw per-story computed styles (~60 MB). Regenerated by `npm run capture`; only diff.mjs reads them.
data/stories/
# Screenshots are published to Pages by .github/workflows/refresh.yml, not committed.
data/shots/
_site/
